# walq 0.3.0

- Inspect and list jobs with `queue.get()` and `queue.list()`.
- Manage jobs with `queue.retry()`, `queue.cancel()`, `queue.reschedule()`, and `queue.remove()`.
- The SQLite adapter migrates existing databases from schema v2 to v3 on open.
